//引入vue组件
import  Vue from 'vue'
//引入vue-router组件，安装的时候如果是vuecli 2那就安装@3版本的router，vuecli3就装@4
import VueRouter from 'vue-router'

//创建一个router实例对象，去管理一组一组的路由规则
const router = new VueRouter({
    routes:[

    ]
    
})

//暴露路由规则
export default router